- package:
    name: "{{ item }}"
    state: present
  with_items:
     - ffmpeg
     - gifsicle
     - libvirt
     - vagrant-libvirt
     - virt-what
     - ansible
     - dnf-utils
     - manifest-tool
     - git-lfs

- include_tasks: virtualbox.yml
