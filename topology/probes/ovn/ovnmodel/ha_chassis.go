//go:generate go run github.com/skydive-project/skydive/graffiti/gendecoder -package github.com/skydive-project/skydive/topology/probes/ovn/ovnmodel
//go:generate go run github.com/mailru/easyjson/easyjson $GOFILE

// Code generated by ovnmetagen
// DO NOT EDIT.

package ovnmodel

import (
	"encoding/json"
	"fmt"

	"github.com/skydive-project/skydive/graffiti/getter"
	"github.com/skydive-project/skydive/graffiti/graph"
)

// HAChassis defines the type used by both libovsdb and skydive for table HA_Chassis
// easyjson:json
// gendecoder
type HAChassis struct {
	UUID        string            `ovsdb:"_uuid" json:",omitempty" `
	ChassisName string            `ovsdb:"chassis_name" json:",omitempty" `
	ExternalIDs map[string]string `ovsdb:"external_ids" json:",omitempty" `
	Priority    int               `ovsdb:"priority" json:",omitempty" `

	ExternalIDsMeta graph.Metadata `json:",omitempty" field:"Metadata"`
}

func (t *HAChassis) Metadata() graph.Metadata {
	// Generate Metadata from maps
	t.ExternalIDsMeta = graph.NormalizeValue(t.ExternalIDs).(map[string]interface{})

	return graph.Metadata{
		"Type":    "HAChassis",
		"Manager": "ovn",
		"UUID":    t.GetUUID(),
		"Name":    t.GetName(),
		"OVN":     t,
	}
}

func (t *HAChassis) GetUUID() string {
	return t.UUID
}

func (t *HAChassis) GetName() string {
	if name := t.UUID; name != "" {
		return name
	}
	return t.GetUUID()
}

// HAChassisDecoder implements a json message raw decoder
func HAChassisDecoder(raw json.RawMessage) (getter.Getter, error) {
	var t HAChassis
	if err := json.Unmarshal(raw, &t); err != nil {
		return nil, fmt.Errorf("unable to unmarshal HAChassis metadata %s: %s", string(raw), err)
	}
	return &t, nil
}
