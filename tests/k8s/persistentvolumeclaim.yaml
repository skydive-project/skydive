kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: skydive-test-persistentvolumeclaim
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 3M
