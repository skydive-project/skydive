kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: skydive-test-persistentvolumeclaim
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 3M
